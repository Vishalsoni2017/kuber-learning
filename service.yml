kind: Service
apiVersion: v1
metadata:
    name: svc 
spec:
    selector:
        env: dev
    type: NodePort
    ports:
        -   protocol: TCP
            port: 80
            targetPort: 80
            nodePort: 30001